/**
 * @author Kayla Martell
 * @desc Provide a list of songs
 */




const INITIAL_STATE = { all: {}, selected: null };

 export default (state = INITIAL_STATE, action) => {
   switch(action.type){
     default:
      return state;
   }
 }
